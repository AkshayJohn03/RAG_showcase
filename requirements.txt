# --- core ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-multipart>=0.0.9
numpy>=1.26

# --- retrieval (all optional except numpy; graceful fallbacks built in) ---
# sentence-transformers gives best quality; system works without it (hash fallback)
sentence-transformers>=2.6
# rank-bm25 preferred; pure-python fallback exists if missing
rank-bm25>=0.2.2
# flashrank: ONNX neural reranker (TinyBERT) — CPU-fast, no torch needed
flashrank>=0.2.0
qdrant-client>=1.9

# --- parsing ---
pypdf>=4.2
python-docx>=1.1

# --- scale corpus fetch (20 Newsgroups via sklearn) ---
scikit-learn>=1.3

# --- llm (optional) ---
openai>=1.30
ollama>=0.2

# --- verification / observability (env-gated, safe without keys) ---
langfuse>=2.0

# --- dev ---
pytest>=8.2
httpx>=0.27
